<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.e.usersdata">

    <application
        android:name=".data.remote.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.todosActivity.TODOsByUserIdActivity" />
        <activity android:name=".ui.activity.todosActivity.TODOsListActivity" />
        <activity android:name=".ui.activity.commentsActivity.CommentsByPostIdActivity" />
        <activity android:name=".ui.activity.commentsActivity.CommentsListActivity" />
        <activity android:name=".ui.activity.photosActivity.PhotosByAlbumIdActivity" />
        <activity android:name=".ui.activity.photosActivity.PhotosListActivity" />
        <activity android:name=".ui.activity.postsActivity.PostsByUserIdActivity" />
        <activity android:name=".ui.activity.postsActivity.PostsListActivity" />
        <activity android:name=".ui.activity.albumsActivity.AlbumsByUserIdActivity" />
        <activity android:name=".ui.activity.albumsActivity.AlbumsListActivity" />
        <activity android:name=".ui.activity.ShimmerFacebookRV" />
        <activity android:name=".ui.activity.usersActivity.UsersListActivity" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE"
        tools:ignore="ManifestOrder" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>